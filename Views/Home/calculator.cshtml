
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="~/css/homepage.css" asp-append-version="true" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/images/littledumbbell.png">
    <title>Tutoring Calculator</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</head>
<body>
<h2>
    <img src="~/images/littledumbbell.png" alt="Dumbbell Icon" width="30" height="30">
    Sarah's Workout Plan and Goals</h2>
<div class="container">
    <h2>Personal Training - Rates Calculator</h2>
    <label for="hours">Enter Hours Needed:</label>
    <input type="number" id="hours" min="1">
    <p class="error" id="error-msg"></p>
    <label for="rate">Hourly Rate:</label>
    <input type="text" id="rate" value="$25" disabled>
    <button id="calculate">Calculate Total</button>
    <h3>Total Cost: <span id="total"></span></h3>
    <a asp-controller="Home" asp-action="Homepage">Back to Homepage</a>

</div>
<footer>
    <p>&copy; 2025 Sarah Durham. All rights reserved.</p>
    <p>Built by Sarah</p>
</footer>

<script>
    $(document).ready(function () {
        $("#calculate").click(function () {
            let hours = parseFloat($("#hours").val());
            let rate = 25; // Set hourly rate

            if (isNaN(hours) || hours <= 0) {
                $("#error-msg").text("Please enter a valid positive number.");
                $("#total").text("");
            } else {
                $("#error-msg").text("");
                let total = hours * rate;
                $("#total").text("$" + total.toFixed(2));
            }
        });
    });
</script>
</body>
</html>
